using UnityEngine;

[ExecuteInEditMode]
public class ArrayModifier : MonoBehaviour
{
    //[Range(1, 3)] int Count = 2;
    //public Vector3 Offset = new Vector3(2, 0, 0);

    //private GameObject[] _clones;
    //private int _lastCount;
    //private Vector3 _lastOffset;

    //void Start()
    //{
    //    if (_lastCount != Count || _lastOffset != Offset)
    //    {
    //        if (_clones != null)
    //        {
    //            foreach (var clone in _clones)
    //            {
    //                DestroyImmediate(clone);
    //            }
    //        }

    //        _clones = new GameObject[Count];
    //        for (int i = 0; i < Count; i++)
    //        {
    //            _clones[i] = Instantiate(gameObject, transform.position + Offset * i, transform.rotation);
    //            _clones[i].transform.parent = transform;
    //        }

    //        _lastCount = Count;
    //        _lastOffset = Offset;
    //    }
    //}
}
